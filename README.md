# APP BUFFET

Esta aplicación está desarrollada utilizando python en una primer instancia, se le va a agregar una base de datos SQL en la cual se registrarán las ventas y los productos para ir manteniendo el stock regulado.

Lógica --> Python
GUI --> TkInter
Base de Datos --> SQL

```python
    pip freeze > requirements.txt # Para armar el archivo de requerimientos

    pip install -r requirements.txt # Para instalar los requerimientos del proyecto
```


